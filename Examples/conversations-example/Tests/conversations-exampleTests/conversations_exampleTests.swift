import XCTest
@testable import conversations_example

final class conversations_exampleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(conversations_example().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
